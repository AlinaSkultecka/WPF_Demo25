<Window x:Class="WPFDemo25.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDemo25"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!--Menu-->
        <Rectangle Grid.Row="0" Grid.ColumnSpan="3" Fill="LightGreen"/>

        <!--Content-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>

            <Rectangle Grid.Column="0" Fill="LightBlue"/>
            <Rectangle Grid.Column="1" Fill="DarkCyan"/>
            <Rectangle Grid.Column="2" Fill="LightBlue"/>

        </Grid>

        <!--Footer-->
        <Rectangle Grid.Row="2" Grid.ColumnSpan="3" Fill="LightPink"/>

    </Grid>
</Window>
